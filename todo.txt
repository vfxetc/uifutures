
- growl (or QtApplication.notify) when done

- set_progress(value, maximum=None, task=None, status=None)
- set_status(message)
- notify(message)
- user_cancelled()


- uifutures.Executor(title='Job Queue', app_key='default')
    - eventually reconnect to /tmp/uifutures.{app_key}.sock
    - set app name
    - set app icon

- dependencies

- spawn an independant process for each actual job, just like I did for qbfutures, so that we don't have to worry about code reloading, threading, etc.

